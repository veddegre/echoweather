// Optional app overrides (see committed config.js — edit locally if needed).
window.ECHO_WEATHER = window.ECHO_WEATHER || {};
// Server keys live in config.local.php on the server (never rsync from laptop).
// See config.example.php for: airnow_api_key, google_pollen_api_key,
// pollen_cache_ttl, pollen_cache_grid, pollen_daily_limit
